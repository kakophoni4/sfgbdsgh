# Полный план: парсер и оценка продающихся компаний

## Цель

Собрать из профильных Telegram-чатов объявления о продаже ООО → пробить по чек-листу → оценить выгодность → отдать заказчику отсортированную таблицу.

Источник MVP: чат **«Продажа компаний, готовые ООО»** (`-1001909540858`).

---

## Архитектура

```
Telethon (session)
    → extract (лоты из текста)
    → SQLite
    → dedup
    → enrich (ЕГРЮЛ → ФНС/ФССП/ЕФРСБ/ЗСК/Федресурс)
    → score / LLM (колонка X)
    → Excel чек-лист
```

---

## Фазы

### Фаза 0 — доступ ✅
- Telethon + `USE_OFFICIAL_API=desktop`
- Сессия `telegram_session.session`
- Логин: `python telegram_login.py`

### Фаза 1 — парсер чата ✅
- History + live
- Сплит мультипостов
- Поля: название, ИНН/ОГРН, дата, цена, СНО, ОКВЭД, ЗСК-claim, продавец, ссылка
- Отсев каталожной рекламы
- Скоринг по тексту объявления
- Excel A–X

Команды:
```bash
python run_parser.py --limit 300
python run_parser.py --listen
```

### Фаза 2 — дедуп + ЕГРЮЛ ✅ (код готов; нужен доступ к nalog.ru)
- Дедуп по ИНН / ОГРН / имя+цена
- Резолв через `egrul.nalog.ru`: ИНН, ОГРН, адрес, директор, дата рег., статус
- Автозаполнение: B, C, F(адрес/директор), H, M, N, W
- Пересчёт score с `confidence=high` после ЕГРЮЛ
- Если DNS/`egrul.nalog.ru` недоступен (VPN/провайдер) — enrich вернёт error, парсер и Excel всё равно работают

Команды:
```bash
python run_parser.py --enrich-only --enrich-limit 30
python run_parser.py --export-only
```

### Фаза 3 — финансы БФО ✅ (код; нужен доступ к bo.nalog.ru)
| Колонка | Источник | Автомат |
|---------|----------|---------|
| K займы/кредиторка | `bo.nalog.ru` строки 1410/1510/1520 | да |
| R обороты >500к | `bo.nalog.ru` строка 2110 | да |
| U отчётность сдана | наличие периодов БФО | да |

```bash
python run_parser.py --enrich-buh --enrich-limit 20
```

### Фаза 3b — ФССП + КАД ✅ (код; на сервере проверить)
| L долги/ИЛ | ФССП (часто капча → ПРОВЕРИТЬ) | полуавто |
| P арбитраж | КАД SearchInstances | полуавто |

```bash
python run_parser.py --rescore
python run_parser.py --enrich-kad --enrich-fssp --enrich-limit 20
```

### Фаза 4 — риски учредителей
| Колонка | Источник |
|---------|----------|
| I недостоверки | выписка ЕГРЮЛ / признаки недостоверности |
| O банкрот/дисквал/санкции | ЕФРСБ + `service.nalog.ru/disqualified.do` + санкционные списки |
| V лизинг/кредиты | fedresurs.ru |

### Фаза 5 — ЗСК (S)
- Официальный светофор, не заявление продавца
- Часто капча → полуавто / ручной ввод / платный пробив
- Красный = hard-reject в score

### Фаза 6 — ручной слой
Всегда вручную: **E, J, Q, T** (+ дожим F при необходимости).  
В Excel жёлтые/пустые = работа оператора.

### Фаза 7 — ИИ (X)
- Вход: JSON строки чек-листа
- Выход: ДА / НЕТ / СОМНИТЕЛЬНО + причины + score
- Hard-rules в коде важнее LLM (красный ЗСК, ликвидация, исключение)
- Сортировка Excel по score/verdict

### Фаза 8 — прод
- Live 24/7 (`--listen`)
- Очередь обогащения с rate-limit
- Алерты в Telegram: новые лоты с вердиктом ДА
- Бэкап `data/listings.db`

---

## Маппинг колонок чек-листа

| Col | Поле | Сейчас | Цель |
|-----|------|--------|------|
| A | Название | TG / ЕГРЮЛ | авто |
| B | ИНН | TG / ЕГРЮЛ | авто |
| C | Дата рег. | TG / ЕГРЮЛ | авто |
| D | Цена | TG | авто |
| E | Цена покупки | — | вручную |
| F | Рег.+адрес | ЕГРЮЛ адрес/директор | авто + ручной коммент |
| G | СНО | TG (позже ФНС) | авто |
| H | Рег до 2024 | из C | авто |
| I | Достоверность | — | ЕГРЮЛ/ФНС |
| J | Первичка/1С | подсказка из TG | вручную |
| K | Займы | — | ФНС бо |
| L | Долги/ИЛ | подсказка TG | ФНС+ФССП |
| M | Не ликвидация | ЕГРЮЛ | авто |
| N | Не исключение | ЕГРЮЛ | авто |
| O | Банкрот/дисквал | — | ЕФРСБ+ФНС |
| P | Суды | — | КАД/ФНС |
| Q | Р/с | подсказка TG | вручную |
| R | Обороты | TG claim | ФНС бо |
| S | ЗСК | заявление продавца | официальный ЗСК |
| T | Приостановки | подсказка TG | вручную / bi.do |
| U | Отчётность | — | ФНС |
| V | Лизинг | — | Федресурс |
| W | Дата проверки | авто | авто |
| X | Результат | эвристика → LLM | авто |

---

## Реалистичные ограничения

1. **~70–80% постов без ИНН** («по запросу») — без ИНН глубокий пробив невозможен.
2. **ЕГРЮЛ/ФНС капчат** при частых запросах → паузы 1.5–2с, лимиты батчей.
3. **ЗСК** почти всегда полуавто без платного API.
4. Заявление продавца ≠ факт — в Excel S помечается как «заявление», пока нет официального пробива.
5. Платный агрегатор (Контур/Spark/DaData) ускорит фазы 3–5 в разы, но не обязателен для старта.

---

## Порядок работ дальше

1. ✅ Дедуп + ЕГРЮЛ (M/N/C/F/B)
2. ⬜ Бухотчётность ФНС → K, R, U
3. ⬜ ФССП + недостоверки → L, I
4. ⬜ ЕФРСБ / дисквал / Федресурс → O, V
5. ⬜ ЗСК semi-auto → S
6. ⬜ LLM-вердикт → X
7. ⬜ Алерты в TG по «ДА»

---

## Команды (шпаргалка)

```bash
python telegram_login.py
python smoke_check.py                 # проверка ФНС/ЕГРЮЛ/БФО на сервере
python run_parser.py --selftest
python run_parser.py --limit 400
python run_parser.py --enrich-only    # паузы из .env ENRICH_PAUSE/JITTER
python run_parser.py --export-only
python run_parser.py --listen
```

Деплой на сервер: см. `SERVER.md` / `deploy/`.

Артефакты:
- `data/listings.db` — база
- `data/checklist_export.xlsx` — выдача заказчику
